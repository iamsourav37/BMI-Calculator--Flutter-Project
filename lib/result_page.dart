import 'package:flutter/material.dart';


class ResultPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("BMI Calculator by Sourav"),
        ),
        body: Center(
          child: Text("Result page is under construction"),
        ),
    );
  }
}